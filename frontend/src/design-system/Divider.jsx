import styled from 'styled-components';

const Divider = styled.hr`
  border: none;
  border-bottom: 1px solid var(--theplayground-black-color, #121212);
  margin: 1.25rem 0;
`;

export default Divider;
